import {
  requestToPay,
  verifyAccountHolder,
  requestToPayTransaction,
} from "./collections";

import {
  checkTransferStatus,
  transfer,
  validateAccountStatus,
} from "./disbursement";

export default {
  requestToPay,
  verifyAccountHolder,
  requestToPayTransaction,
  transfer,
  checkTransferStatus,
  validateAccountStatus,
};
